# Zephyr QEMU Application — Traffic Sign Recognition
CONFIG_CPP=y
CONFIG_STD_CPP17=y

# TFLM
CONFIG_TENSORFLOW_LITE_MICRO=y

# Serial
CONFIG_SERIAL=y
CONFIG_UART_INTERRUPT_DRIVEN=y

# Memory — tuned for minimal footprint
CONFIG_HEAP_MEM_POOL_SIZE=2048
CONFIG_MAIN_STACK_SIZE=8192
